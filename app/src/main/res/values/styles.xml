<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/blue</item>
        <item name="android:colorBackground">@color/gray_space</item>
        <item name="android:windowBackground">@color/gray_space</item>
        <item name="android:backgroundSplit">@color/gray_space</item>
        <item name="android:listDivider">@drawable/item_divider</item>
    </style>
    <style name="AppTheme.Night" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/blue</item>
        <item name="android:colorBackground">@color/gray_space</item>
        <item name="android:windowBackground">@color/gray_space</item>
        <item name="android:backgroundSplit">@color/gray_space</item>
        <item name="android:listDivider">@drawable/item_divider</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
